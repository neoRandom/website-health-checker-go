info:
  name: First Site, if any
  type: http
  seq: 1

http:
  method: DELETE
  url: http://localhost:8080/api/v1/site/1
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
