info:
  name: Update Site
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:8080/sites/list
  body:
    type: json
    data: |-
      {
        "id": 1,
        "url": "https://youtube.com",
        "expected_status_code": 201
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
