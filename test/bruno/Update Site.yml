info:
  name: Update Site
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/sites/list
  body:
    type: json
    data: |-
      {
        "id": 1,
        "url": "https://youtube.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
