info:
  name: Add Site
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/sites/list
  body:
    type: json
    data: |-
      {
        "url": "https://google.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
